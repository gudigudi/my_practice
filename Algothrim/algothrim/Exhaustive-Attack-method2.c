#include <stdio.h>
#define SIDE_N 3
#define LENGTH 3
#define VARIABLES 6
int A, B, C, D, E, F;
int *pt[] {&A, &B, &C, &D, &E, &F};
int *side[SIDE_N]{LENGTH} = {&A, &B, &C, &D, &E, &F, &A};
int side_total[SIDE_N];

int main(void)
{
    int
    return 0;
}
