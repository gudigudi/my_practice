// pipe example in Linux

#include <sys/wait.h>
#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, char *argv[])
{
    int pipe_fd[2]; // read end and write end of Pipe
    pid_t child_pid; //subprocess
    char pipe_buf; // pip data
    memset()
    return 0;
}
